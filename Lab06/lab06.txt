6.1

a.
Curl -I couldn't test HEAD when I use Curl to test it. So I used -head instead and it worked successfully. 


Chrome: GET was successful but all the other methods were not working. 



b. What is the most appropriate HTTP response code for pages that aren’t defined by an Express route?

-200 

6.2 
a. it supports GET and POST.
b. The data is in a json format and is not modified. 

